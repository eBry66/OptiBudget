import { describe, it, expect } from 'vitest';

// [AC-001] mentioned here only as a comment, never as the real test title.
describe('fixture', () => {
  it('does the thing', () => {
    expect(true).toBe(true);
  });
});
