# Session handoff — 2026-08-14

OptiBudget handoff — continuing from a prior session, repo is authoritative.

## Verified repo state

- main is genuinely resting: active_task: none, attempt: 0,
  authorized_branch: main, gate: 1.
- task-001 (CI + validators) and task-002 (resting-state invariant in
  validate-state.mjs/validate-task.mjs) are both merged and closed. PR #6
  and PR #8. DECISIONS.md has entries for both.
- GitHub CLI (gh) is installed and authenticated on this machine (Windows,
  PowerShell, C:\Dev\OptiBudget). Use it directly for PR review/merge.
  Known constraint: `gh pr review --approve` fails with "Can not approve
  your own pull request" since the repo owner is both author and reviewer
  on every PR — use `gh pr review --comment` instead, with the decision
  recorded in the body text. Structural fact about solo-repo GitHub, not a
  bug to route around differently each time.
- Known, standing, unresolved defect: scripts/check-coverage.mjs requires
  100% AC-id test coverage unconditionally, with no bootstrap/release
  exception, so the `unit` required CI check cannot pass on any PR until
  the full application exists. True since task-001, logged in
  DECISIONS.md (2026-08-13 entry). Every merge until a TESTING.md
  (DOC-017) revision happens needs the same `gh pr merge --admin`
  override, same "Accepted deviations" reasoning restated. Expected, not
  a new problem each time.

## Not yet done — drafted and reviewed, never committed

1. orchestration/PROJECT_RULES.md revision (execution-tier model: Tier 1 =
   autonomous implement/review/CI up to an authorized-SHA merge, Tier 2 =
   HITL confirms before implementation). Full text drafted, went through
   several rounds of critique/correction. Not in the repo. Known
   correction needed before pasting: the draft says "HITL submits an
   approving Review" as the Tier 1 authorization mechanism — needs
   correcting given the self-approval constraint above.
2. orchestration/HITL_GUIDE.md — a new §13.9 (project-close /
   next-project template-propagation policy). Drafted, not committed.
3. A brief for Claude Code to extend validate-task.mjs (or add
   validate-tier.mjs) with structural Tier 1/Tier 2 verification.
   Drafted, not handed off.
4. A deferred-governance-task DECISIONS.md line, recording that
   ACCEPTANCE.md/TESTING.md's manual-AC-confirmation model should be
   reviewed once tier automation is running. Not written yet.

## Next task, not started

task-003 (open-task/close-task automation for project.state.yaml
transitions, extending scripts/next.mjs). orchestration/tasks/task-003.yml
does not exist yet. Brief was fully drafted and handed to Claude Code as
a prompt, but Claude Code was never given the go-ahead to start. Scope:
extend next.mjs, don't create a parallel script; it does not commit or
push anything itself — that stays HITL-executed.