# Session handoff — 2026-08-14

OptiBudget handoff — continuing from a prior session, repo is authoritative.

## Verified repo state

- main is genuinely resting: active_task: none, attempt: 0,
  authorized_branch: main, gate: 1.
- task-001 (CI + validators) and task-002 (resting-state invariant in
  validate-state.mjs/validate-task.mjs) are both merged and closed. PR #6
  and PR #8. DECISIONS.md has entries for both.
- GitHub CLI (gh) is installed and authenticated on this machine (Windows,
  PowerShell, C:\Dev\OptiBudget). Use it directly for PR review/merge.
  Known constraint: `gh pr review --approve` fails with "Can not approve
  your own pull request" since the repo owner is both author and reviewer
  on every PR — use `gh pr review --comment` instead, with the decision
  recorded in the body text. Structural fact about solo-repo GitHub, not a
  bug to route around differently each time.
- Known, standing, unresolved defect: scripts/check-coverage.mjs requires
  100% AC-id test coverage unconditionally, with no bootstrap/release
  exception, so the `unit` required CI check cannot pass on any PR until
  the full application exists. True since task-001, logged in
  DECISIONS.md (2026-08-13 entry). Every merge until a TESTING.md
  (DOC-017) revision happens needs the same `gh pr merge --admin`
  override, same "Accepted deviations" reasoning restated. Expected, not
  a new problem each time.

## Not yet done — drafted and reviewed, never committed

1. orchestration/PROJECT_RULES.md revision (execution-tier model: Tier 1 =
   autonomous implement/review/CI up to an authorized-SHA merge, Tier 2 =
   HITL confirms before implementation). Full text drafted, went through
   several rounds of critique/correction. Not in the repo. Known
   correction needed before pasting: the draft says "HITL submits an
   approving Review" as the Tier 1 authorization mechanism — needs
   correcting given the self-approval constraint above.
2. orchestration/HITL_GUIDE.md — a new §13.9 (project-close /
   next-project template-propagation policy). Drafted, not committed.
3. A brief for Claude Code to extend validate-task.mjs (or add
   validate-tier.mjs) with structural Tier 1/Tier 2 verification.
   Drafted, not handed off.
4. A deferred-governance-task DECISIONS.md line, recording that
   ACCEPTANCE.md/TESTING.md's manual-AC-confirmation model should be
   reviewed once tier automation is running. Not written yet.

## Next task, not started

task-003 (open-task/close-task automation for project.state.yaml
transitions, extending scripts/next.mjs). orchestration/tasks/task-003.yml
does not exist yet. Brief was fully drafted and handed to Claude Code as
a prompt, but Claude Code was never given the go-ahead to start. Scope:
extend next.mjs, don't create a parallel script; it does not commit or
push anything itself — that stays HITL-executed.

---

## Update — later session, same day (2026-08-14)

Everything in "Not yet done" above is now done, committed, and approved.
This section supersedes that list; the items above are historical record of
what was drafted, not current status.

### Completed and committed this session

- `product/DECISIONS.md` (DOC-010): three entries appended — resting-state
  `allowed_paths` supersession, coverage-check terminology correction
  (`check-coverage.mjs` behaves to spec; the open question is contract
  design, not a script defect), dependency-tracking decision (two-phase:
  append-only `engineering/DEPENDENCIES.md` now, DOC-id status deferred to
  v2). Approved.
- `orchestration/PROJECT_RULES.md` (DOC-023): full Tier-1/Tier-2 execution
  model committed. Tier-1 merge authorization corrected from the earlier
  "HITL submits an approving Review" draft (blocked by self-approval) to
  align with `HITL_GUIDE.md` §17.3 — which turned out to already be
  committed but unapproved, discovered mid-session. Also adds the
  dependency-tracking constraint and a standing rule: a task that
  intentionally diverges from a `DECISIONS.md` entry isn't closed until a
  new entry records the supersession. Approved.
- `project.state.yaml` (DOC-024) and `orchestration/HITL_GUIDE.md`
  (DOC-026): both found modified-but-unapproved on `main` from earlier
  work (task-001/002 close, §17 review-calibration corrections) — approved
  this session after verifying via `git diff` against each one's last
  approved commit. DOC-024 additionally went `stale` after DOC-023's
  re-approval (expected, per §5.1) and was re-approved.
- `node scripts/next.mjs check` → CHECK OK.

### New finding — `--admin` override may no longer be needed for
non-application PRs

Merging the PR carrying DOC-010/023/024/026 (documentation only, zero ACs
touched), the `unit` check was red as expected (same standing
`check-coverage.mjs` condition, unchanged), but **the merge button was not
blocked** — no forced approving-review rule, and `unit` wasn't a required
check for this merge. Plain "Merge pull request" worked, no `--admin`
needed. This is one data point, not a confirmed branch-protection
configuration — check GitHub Settings → Branches directly to see the
actual required-checks list before assuming `--admin` is unnecessary on
future PRs, especially ones where `unit` genuinely should be required
(application-code PRs, once they exist).

### New open item — template provenance mismatch (unresolved)

- `template.lock`: `template_version: 0.0.0`, `synced_at: unset`,
  placeholder repo URL still literally `YOUR-USERNAME/dev-template.git`.
- `template.manifest.yaml`: `template_version: 1.2.0`.
- `dev-template` repo: unresolved via GitHub API (404) — inconclusive,
  could be inaccessible rather than nonexistent.
- Not necessarily contradictory (manifest can describe working-tree
  machinery, lock describes sync provenance) but the connection between
  the two is currently unexplained. Needs PC-side investigation:
  `gh repo view dev-template`, `git log` on both files. Do not edit either
  file to force agreement. Record the resulting decision in `DECISIONS.md`
  once actually resolved — not before.

### New open item — HITL_GUIDE.md has no documented procedure for editing
a DOC-graph document after gate-0 exit / branch protection is on

Section 5's loop assumes direct pushes to `main`. This session had to
improvise branch → push → PR → fill description → merge for a routine
document edit, because nothing in the guide covers this case. Candidate
addition to `HITL_GUIDE.md`.

### Housekeeping — not yet done

Two stale branches, local and remote, sitting at already-merged commits:
`decisions-task-002-note-v2`, `hitl-guide-17-review-calibration`.

```powershell
git branch -d decisions-task-002-note-v2 hitl-guide-17-review-calibration
git push origin --delete decisions-task-002-note-v2 hitl-guide-17-review-calibration
```

### task-003 — still not started, unchanged from above

### Immediate next steps, in order

1. Dependency-tracking brief to Claude Code (`engineering/DEPENDENCIES.md`)
   — unblocked, ready to send.
2. Tier-validator brief to Claude Code — unblocked, ready to send.
3. Template provenance reconciliation (PC-side).
4. Branch-protection ruleset confirmation (GitHub Settings → Branches).
5. task-003.